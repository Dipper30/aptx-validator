export * from './types';

import { boolean, number, string, object, array, any, and, or } from './types';

const v = {
  boolean,
  number,
  string,
  object,
  array,
  any,
  and,
  or,
};

export { v };

export default v;
